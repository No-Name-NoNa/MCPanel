package moe.mcg.mcpanel.api.pack;

public record ServerInfo(String serverName,
                         String serverIntro,
                         String serverVersion,
                         String playerCount) {
}
